packages:
  - 'packages/*'
  - 'apps/*'

# pnpm 11 no longer reads the "pnpm" field in package.json — settings live here.
# Dependencies allowed to run install scripts, as a deliberate, visible allowlist.
onlyBuiltDependencies:
  - esbuild
